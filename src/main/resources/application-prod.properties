
spring.data.mongodb.uri=mongodb+srv://mandalnisha888:mongodblove@123@cluster0.kfisyg8.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0&connectTimeoutMS=10000&socketTimeoutMS=10000
# Server port (Render requires this)
server.port=8080

# Security - Keep Swagger enabled but consider adding auth
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true

# Production optimizations
spring.data.mongodb.auto-index-creation=true
server.tomcat.threads.max=50
spring.jpa.show-sql=false

# Health check for Render
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# Full security disable (only for prod where you're sure)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration,org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
spring.security.filter.disabled=true

# Actuator configuration
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=health
management.health.mongo.enabled=false
